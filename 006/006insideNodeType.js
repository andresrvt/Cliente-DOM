/* 006insideNodeType.html/.js: ¿Qué muestra el script? */

"use strict";

// El resultado es 1, debido a que cuando se ejecuta la etiqueta script, el último nodo es el mismo script, ya que el navegador todavía no ha procesado el resto de la página.